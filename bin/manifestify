#!/usr/bin/env node

var manifestify = require('../');

var dir = process.argv[2];
var pattern = process.argv[3];

if (!dir || !pattern) {
	return console.error('syntax: manifestify <directory> <file-pattern>');
}

manifestify(dir, pattern).on('error', console.error).pipe(process.stdout);